# Monorepo npm configuration
# Disable legacy-peer-deps to avoid npm warnings
legacy-peer-deps=false

# Ensure workspace:* is resolved to actual versions during pnpm publish
# This is the default behavior but we're being explicit
link-workspace-packages=true
