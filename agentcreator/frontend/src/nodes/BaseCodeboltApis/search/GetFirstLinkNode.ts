import { BaseGetFirstLinkNode } from '@agent-creator/shared-nodes';

// Frontend GetFirstLink Node - UI only
export class GetFirstLinkNode extends BaseGetFirstLinkNode {
  constructor() {
    super();
  }
}