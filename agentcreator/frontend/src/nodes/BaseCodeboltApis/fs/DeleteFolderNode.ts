import { BaseDeleteFolderNode } from '@agent-creator/shared-nodes';

// Frontend DeleteFolder Node - UI only
export class DeleteFolderNode extends BaseDeleteFolderNode {
  constructor() {
    super();
  }
}