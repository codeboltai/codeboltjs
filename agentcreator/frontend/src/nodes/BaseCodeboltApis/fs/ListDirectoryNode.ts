import { BaseListDirectoryNode } from '@codebolt/agent-shared-nodes';

// Frontend ListDirectory Node - UI only
export class ListDirectoryNode extends BaseListDirectoryNode {
  constructor() {
    super();
  }
}