import { BaseSummarizePreviousRequestNode } from '@codebolt/agent-shared-nodes';

// Frontend SummarizePreviousRequest Node - UI only
export class SummarizePreviousRequestNode extends BaseSummarizePreviousRequestNode {
  constructor() {
    super();
  }
}