import { BaseExecuteToolRequestNode } from '@agent-creator/shared-nodes';

// Frontend ExecuteToolRequest Node - UI only
export class ExecuteToolRequestNode extends BaseExecuteToolRequestNode {
  constructor() {
    super();
  }
}
