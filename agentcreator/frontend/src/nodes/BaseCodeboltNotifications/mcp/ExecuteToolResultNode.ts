import { BaseExecuteToolResultNode } from '@agent-creator/shared-nodes';

// Frontend ExecuteToolResult Node - UI only
export class ExecuteToolResultNode extends BaseExecuteToolResultNode {
  constructor() {
    super();
  }
}
