import { BaseFolderCreateRequestNode } from '@agent-creator/shared-nodes';

// Frontend FolderCreateRequest Node - UI only
export class FolderCreateRequestNode extends BaseFolderCreateRequestNode {
  constructor() {
    super();
  }
}