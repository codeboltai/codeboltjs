import { BaseFileReadRequestNode } from '@codebolt/agent-shared-nodes';

// Frontend FileReadRequest Node - UI only
export class FileReadRequestNode extends BaseFileReadRequestNode {
  constructor() {
    super();
  }
}