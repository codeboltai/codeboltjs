import { BaseFolderDeleteRequestNode } from '@agent-creator/shared-nodes';

// Frontend FolderDeleteRequest Node - UI only
export class FolderDeleteRequestNode extends BaseFolderDeleteRequestNode {
  constructor() {
    super();
  }
}