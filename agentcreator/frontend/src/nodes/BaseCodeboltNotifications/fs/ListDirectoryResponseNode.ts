import { BaseListDirectoryResponseNode } from '@codebolt/agent-shared-nodes';

// Frontend ListDirectoryResponse Node - UI only
export class ListDirectoryResponseNode extends BaseListDirectoryResponseNode {
  constructor() {
    super();
  }
}