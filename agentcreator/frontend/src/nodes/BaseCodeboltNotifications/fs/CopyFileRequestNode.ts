import { BaseCopyFileRequestNode } from '@codebolt/agent-shared-nodes';

// Frontend CopyFileRequest Node - UI only
export class CopyFileRequestNode extends BaseCopyFileRequestNode {
  constructor() {
    super();
  }
}