import { BaseSearchRequestNode } from '@codebolt/agent-shared-nodes';

// Frontend SearchRequest Node - UI only
export class SearchRequestNode extends BaseSearchRequestNode {
  constructor() {
    super();
  }
}