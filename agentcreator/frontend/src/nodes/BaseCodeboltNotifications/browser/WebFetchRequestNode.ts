import { BaseWebFetchRequestNode } from '@agent-creator/shared-nodes';

// Frontend WebFetchRequest Node - UI only
export class WebFetchRequestNode extends BaseWebFetchRequestNode {
  constructor() {
    super();
  }
}
