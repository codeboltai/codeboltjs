import { BaseWebSearchRequestNode } from '@agent-creator/shared-nodes';

// Frontend WebSearchRequest Node - UI only
export class WebSearchRequestNode extends BaseWebSearchRequestNode {
  constructor() {
    super();
  }
}
